//------------------------------------------------
// Texture dictionary.
//------------------------------------------------

typedef struct
{
    local uint i;
    int raster_count;
    
    for (i=0; i<raster_count; i++)
        RWSection raster(ST_TextureDictionary);
} RWTextureDictionary;